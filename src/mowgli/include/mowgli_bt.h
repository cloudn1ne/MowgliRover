
#ifndef MOWGLI_BT_H
#define MOWGLI_BT_H

// for now that file holds the global externals used by the various node types
extern bool callSetGpsControlSrv(bool enabled);
//extern bool callMowSrv(bool enabled);
//extern bool getMowStatus(void);
//extern bool getChargingStatus(void);

// global states
//extern bool gstate_blade_motor_enabled;
//extern bool gstate_is_charging;
//extern float gstate_v_battery;


#endif // MOWGLI_BT_H